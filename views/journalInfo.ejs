<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Yee Mon's Travel Journals</title>
</head>

<body>

    <%- include ('partials/navbar.ejs') %>

    <!-- if journal is found -->
    <% if (journal) { %>  
        <div class="container-fluid text-light" style="background-color: #F3EFDE;">

            <div class="container p-5 col-lg-8 col-sm-10">

                <div class="card shadow border-0">
                    <img src="/images/<%= journal.image %>" class="card-img-top" alt="Journalimage">
                    
                    <div class="card-body">

                        <div class="mt-2 mb-3">
                            <div class="badge bg-dark rounded-pill">Location: <%= journal.location %> </div>
                            <div class="badge bg-dark rounded-pill">Travel Period: <%= journal.period %> </div>
                        </div>
                        
                        <h5 class="card-title mt-3 mb-3"> <%= journal.title %> </h5>
                        <p class="card-text mb-2" style="font-size: large;"> <%= journal.text %> </p>

                    </div>

                    <div class="card-footer">
                        <small class="text-body-secondary">Posted on: <%= journal.date.toLocaleDateString() %> </small>
                    </div>


                </div>

                <div>
                    <a href="/" class="btn btn-outline-dark mb-3 mt-4">Back</a>
                </div>

            </div>

        </div>

    <% } else { %>
        <p>No journal found.</p>
    <% } %>

    <%- include ('partials/footer.ejs') %>

</body>
</html>